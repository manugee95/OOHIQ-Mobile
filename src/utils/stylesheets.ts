import { StyleSheet } from "react-native";

export const shadowStyles = StyleSheet.create({
	shadow: {
		shadowOffset: {
			width: 0,
			height: 3,
		},
		shadowOpacity: 0.17,
		shadowRadius: 3.05,
		elevation: 4,
		shadowColor: "#52514F",
	},
});
